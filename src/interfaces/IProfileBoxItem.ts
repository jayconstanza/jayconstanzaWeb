/**
 * @interface IProfileBoxItem
 * @description interface that describes the object passed to the ProfileBox component
 */
export interface IProfileBoxItem {
  iconName?: string;
  title: string;
  description?: string;
  period?: string;
}
